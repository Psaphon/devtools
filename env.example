# Dev Tools API Keys
# Copy to /media/secrets/devtools/env and fill in your values
# install.sh will symlink this to ~/.config/dtl/env
# Permissions: chmod 600 (owner read/write only)

# Required for AI container (Claude Code)
ANTHROPIC_API_KEY=

# Git identity (used inside AI containers for commits)
GIT_AUTHOR_NAME=
GIT_AUTHOR_EMAIL=

# Optional: Telegram bot for remote development
DTL_BOT_TOKEN=
DTL_BOT_CHAT_ID=
